---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import FeatureSection from '../components/FeatureSection.astro';
---

<Layout>
	<Hero />
	<FeatureSection title="Smooth by default." body="Lenis drives the scroll, GSAP ScrollTrigger drives the reveals." />
	<FeatureSection title="Per-component animations." body="Each section owns its own scroll-triggered motion." />
</Layout>
